platform :ios, '17.0'

target 'Easy Shelfie' do
  use_frameworks!

  # Removed Firebase, AppsFlyer and WebView pods per cleanup request
end

target 'notifications' do
  use_frameworks!

  # No pods for notifications target after cleanup
end
